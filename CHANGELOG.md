# Horae 更新日志 / Changelog

> 最近版本的更新内容请查看 [README.zh-CN.md](README.zh-CN.md)

---

## v1.11.19

- Vector recall query is now unified into a single embedding pass with merged format:
  - `[当前情境] <time/location/characters/events from latest AI state>`
  - `[玩家输入] <latest user input>`
- Rerank query now uses the same merged query format instead of user text only.
- State query now includes time (prefer latest AI turn timestamp; fallback to aggregated state time).
- Removed fallback recall runtime path (retry with previous AI response) from vector recall flow.
- Removed fallback controls from settings UI (fallback switch and fallback threshold panel).
- Added runtime log for effective retrieval threshold to help debugging recall behavior.
- 修复楼层 AI 分析时会触发向量召回的问题。
- 将剧情轨迹（Story Timeline）的注入深度调整为聊天记录最上方。

---

## v1.10.0

全面重构 RPG 系统，新增 6 个 RPG 子模块、据点经营系统、装备多种族模板等功能。

### RPG 模式

- **子模块独立开关**：装备栏、声望、等级、货币、据点各为独立 checkbox，按需组合。关闭时零 Token 消耗
- **角色卡标签分页**：角色详情区改为分页标签（属性/技能/装备/声望/货币），仅显示已开启的模块
- **只显示在场角色**：顶部角色卡和底部 HUD 根据 `characters_present` 过滤，离场角色数据保留但不显示

### 新增功能

- **等级/经验值系统**：AI 通过 `level:` / `xp:` 标签输出等级变化。经验值公式：升级所需 = 等级 × 100，等级差 ≥10 的低级挑战仅得 1 点经验
- **货币系统**：自定义币种和兑换比率，每个币种可设置 emoji 图标，AI 通过 `currency:归属|币名=±增量` 修改
- **声望系统**：自定义势力声望分类和数值范围，支持细项（子维度）
- **装备栏系统**：按角色独立格位，内置人类/兽人/有翼族/马人/拉弥亚/恶魔六种预设模板，支持自存模板
- **据点/基地系统**：多层级树形结构，AI 通过 `base:` 标签自动更新，支持手动管理和导出/导入

---

## v1.9.0

### 自动摘要 & 副 API

- **副 API 多轮结构改造**：重构摘要请求消息结构，采用 system/assistant/user 多轮交替格式，大幅提升 NSFW 内容摘要通过率
- **模型下拉选单**：副 API、Embedding、Rerank 三处模型设置均从文本输入改为下拉选单
- **自动摘要警告优化**：不再因部分楼层缺少时间戳就完全阻止摘要

### 向量记忆

- **全文剔除标签**：向量设置中新增「全文剔除标签」输入框，避免小剧场/旁白等非剧情内容被 AI 召回
- **番外消息隔离**：标记为「番外」的消息不再被向量索引和召回

---

## v1.8.0

- **RPG 模式**：开启后 AI 通过 `<horaerpg>` 标签输出角色属性和技能变化，与现有 `<horae>` 标签互不干扰
  - **自定义属性条**：默认 HP/MP/SP 三条，用户可自由增删、修改显示名称和颜色（如改 MP 为「灵力」、新增「精力值」「慾望值」等）
  - **状态异常追踪**：支持昏眩、流血、冰冻、灼烧、石化等数十种状态关键词，自动匹配差异化图标显示
  - **技能系统**：记录技能归属、等级和效果描述，技能与 NPC 角色通过编号联动，支持手动添加和 AI 自动生成
  - **底部 HUD 状态栏**：消息面板上方渲染在场角色的简易状态条，显示属性条和异常状态图标，自动跟随底部栏的宽度和偏移设置
  - **RPG 分页**：顶部面板新增 RPG 标签页，展示属性条配置、各角色详细属性和状态列表、技能列表
  - **条件注入**：关闭 RPG 模式时不注入任何提示词，零 Token 消耗；发送属性条和技能列表可独立开关
- **RPG 面板美化**：自助美化工具新增「RPG 状态栏」区域，支持设置背景色和透明度。RPG HUD 纳入主题系统，跟随日夜模式和自定义主题变量

---

## v1.7.0

- **向量记忆 (Beta)**：基于本地 AI 模型的智能回忆系统，专为搭配「自动摘要 & 隐藏」设计。当对话中涉及历史事件时，插件会自动从被隐藏的旧消息中精准召回相关时间线片段，注入上下文供 AI 参考
  - **全程本地运算**：使用浏览器内置 Web Worker 运行轻量 AI 模型，不消耗额外 API 额度
  - **双模型可选**：`bge-small-zh-v1.5`（中文优化）和 `multilingual-e5-small`（多语言支持）
  - **智能检索**：结合意图识别、结构化查询和语义搜索三层检索策略
  - **全文回顾**：高置信度的召回结果可发送原始正文，让 AI 获得完整的叙事细节

---

## v1.6.0

- **自助美化工具**：可视化美化面板。色相条、饱和度/亮度/日夜模式滑杆、图片装饰。所有修改实时预览，可导出 JSON 分享
- **新手导航**：首次使用自动触发交互式教学
- **副 API 破限注入**：自动摘要的独立 API 请求现在会自动注入 SillyTavern 的 jailbreak 提示词和 `max_tokens` 参数

---

## v1.5.0

### 新增功能

- **情绪/心理状态追踪**：追踪在场角色的情绪变化。Token 消耗极低——AI 仅在情绪发生明显变化时才输出 `mood:` 标签
- **关系网络**：记录角色之间的关系（朋友、恋人、上下级、宿敌等）。展示完全由插件从数据库读取渲染，不消耗 AI 输出 Token
- **场景记忆**：记录地点的固定物理特征，保持跨回合场景描写一致。智能检索——仅发送匹配到的那一条描述
- **自动摘要 & 隐藏**：超过阈值自动触发全文摘要，原始楼层 `/hide` 省 Token
- **独立 API（真并行）**：自动摘要可配置独立的 OpenAI 兼容端点，实现与 AI 回复的真并行

### 改进

- **全局表格数据按卡分离**
- **智能记忆压缩**：多选模式下可将多条事件交给 AI 压缩合并为一条摘要

---

## v1.4.0

- **好感度小数点支持**
- **顶部图标开关**
- **时间线插入功能**：长按时间线事件弹出操作菜单，支持在上方/下方添加事件或插入摘要页
- **表格填写提示词自定义**
- **表格 AI 理解增强**：发送给 AI 的表格数据带有坐标标注
- **表格数据清理修复**

---

## v1.3.0

- **全局表格**：自定义表格新增「全局」和「本地」两种作用域
- **行列锁定**：右键/长按表格单元格可锁定行、列或单个格子
- **主题系统**：内置「樱花粉」「森林绿」「海洋蓝」三套预设主题
- **自助美化**：新增 `THEME.md` 文档
- **日夜模式**
- **自定义 CSS**
- **消息面板宽度**
- **正则置底**
- **时间线多选**

---

## v1.2.0

- **自定义提示词**：设置中新增提示词编辑器
- **AI 摘要功能新增**：可中断生成，新增 NPC 和好感度勾选项
- **摘要审阅弹窗**
- **好感度显示开关**
- **好感度删除**

---

## v1.1.0

- **AI 智能摘要**：批量分析历史消息，自动提取剧情事件和物品
- **Token 计数器**
- **swipe 适配**
- **美化兼容**
- **正则修改**
